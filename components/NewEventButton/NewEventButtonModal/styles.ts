import {StyleSheet} from "react-native";

export const styles = StyleSheet.create({
    inputContainer: {
        flex: 1,
        alignContent: 'center',
        flexDirection: 'row',
        alignItems: 'center',
        paddingLeft: 120,
    },
});